import { Grid, styled } from "@mui/material";

const GridContainerWelcome = styled(Grid)({
  height: "100vh",
  background:
    'url("/images/welcome-background.jpg") center top / cover no-repeat',
});

export default GridContainerWelcome;
